name: Build LaTeX PDFs

on:
  push:
  pull_request:
    paths:
      - 'neurips_paper/**'
  workflow_dispatch: {}

jobs:
  build-pdfs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build main paper with LaTeX
        uses: xu-cheng/latex-action@v2
        with:
          # Compile the main document that includes the checklist
          root_file: neurips_paper/neurips_2026.tex
          # latexmk flags: produce pdf, nonstopmode, halt on error
          latexmk_flags: -pdf -interaction=nonstopmode -halt-on-error

      - name: Upload generated PDF artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: neurips-checklist-pdf
          path: neurips_paper/*.pdf
